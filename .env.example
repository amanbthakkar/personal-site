# Homelab visitor counter API (optional local override)
VITE_API_BASE_URL=https://cloud.amanthakkar.com

# Google Analytics 4 (production builds only; set in GitHub Actions for deploy)
VITE_GA_MEASUREMENT_ID=G-GDRFCMH8L9

# Discover / newsfeed tab (/feed). Unset = on in dev, off in production builds.
# VITE_FRONTEND_NEWSFEED=true
# VITE_FRONTEND_NEWSFEED=false
